{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; echo \"$f\" | grep -qE '/lib/' && cd /home/kdy987/work/pcms-react && fm.sh test; } 2>/dev/null || true",
            "statusMessage": "lib 파일 변경 감지 - 테스트 실행 중..."
          }
        ]
      }
    ]
  }
}
