# ── Database ──────────────────────────────────────────────────────────────
# postgres 컨테이너명으로 접근 (같은 docker-compose 네트워크)
spring.datasource.url=jdbc:postgresql://postgres:5432/kdy987_db
spring.datasource.username=kdy987
spring.datasource.password=kalpa987!

# ── Redis ─────────────────────────────────────────────────────────────────
data.redis.host=redis
data.redis.port=6379
data.redis.password=kalpa987!

# ── JWT ───────────────────────────────────────────────────────────────────
jwt.secret=we_are_the_world_secret_key_for_jwt_12345678901234567890

# ── Logging ───────────────────────────────────────────────────────────────
logging.level.kr.co.kalpa.pcms=INFO
logging.level.org.springframework.security=WARN

# ── DevTools 비활성화 ──────────────────────────────────────────────────────
spring.devtools.restart.enabled=false
spring.devtools.livereload.enabled=false

# ── Swagger 비활성화 ───────────────────────────────────────────────────────
springdoc.swagger-ui.enable=false

# ── 파일 저장 경로 ─────────────────────────────────────────────────────────
kalpa.pcms.base-dir=/data/docker/apps/pcms
file.upload.base-dir=${kalpa.pcms.base-dir}/uploads
file.upload.attach-files-dir=${file.upload.base-dir}/attachments
file.upload.editor-images-dir=${file.upload.base-dir}/editor-images
file.temp-dir=${kalpa.pcms.base-dir}/temp

# ── 파일 서빙 URL ──────────────────────────────────────────────────────────
file.image.base-url=http://jskn.iptime.org/pcms/editor-images/
apnode.file.base-dir=${kalpa.pcms.base-dir}/apnodes
apnode.file.base-url=http://jskn.iptime.org/pcms/apnodes/

# ── Public Data ───────────────────────────────────────────────────────────
public.data.api-key=e5e22c284503db8332064a44cffb10183a349dc92339f026f6ed7d9e0639f84a
